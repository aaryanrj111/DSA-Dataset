"Selected rubric"  
Solution 2: Using LCA – O(n) Time and O(h) Space

"Feedback with marks"  
1. Handle null check and target node checks for LCA [1 mark] - The code correctly handles null checks and target node checks in the `lca` method. [1 mark]  
2. Recursively search left and right subtrees for LCA [1 mark] - The code correctly searches left and right subtrees for LCA in the `lca` method. [1 mark]  
3. Determine LCA based on search results [1 mark] - The code correctly determines the LCA based on search results in the `lca` method. [1 mark]  
4. Return appropriate node [1 mark] - The code returns the appropriate node in the `lca` method. [1 mark]  
5. Handle null check: return -1 if root is null for level finding [1 mark] - The code correctly handles null check in the `minDist` method. [1 mark]  
6. Return level if root.data equals target value [1 mark] - The code correctly returns level if root.data equals target value in the `minDist` method. [1 mark]  
7. Recursively search left and right subtrees, incrementing level [1 mark] - The code correctly searches left and right subtrees, incrementing level in the `minDist` method. [1 mark]  
8. Return appropriate level or -1 based on search results [1 mark] - The code returns the appropriate level or -1 based on search results in the `minDist` method. [1 mark]  
9. Find the LCA of the two nodes [1 mark] - The code correctly finds the LCA of the two nodes in the `findDist` method. [1 mark]  
10. Calculate distance from LCA to first node [1 mark] - The code correctly calculates the distance from LCA to the first node in the `findDist` method. [1 mark]  
11. Calculate distance from LCA to second node [1 mark] - The code correctly calculates the distance from LCA to the second node in the `findDist` method. [1 mark]  
12. Return the sum of these distances [1 mark] - The code correctly returns the sum of these distances in the `findDist` method. [1 mark]  

"Marks Scored out of total marks"  
12 out of 12 marks